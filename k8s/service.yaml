apiVersion: v1
kind: Service
metadata:
  name: "smoketest2"
  namespace: "agents-platform-eng"
spec:
  selector:
    app: "smoketest2"
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
