agent:
  id: "smoketest2"
  team: "platform-eng"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "moderate"

identity:
  sp_client_id: "57066a78-4031-486c-97a1-a01f02951286"
  keyvault_url: "https://kv-agenticdemo-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 5
  window_minutes: 1
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
